#!/bin/bash

# 1. ??????(? 05_21)
current_date=$(date +%-m_%d)

# 2. ??????????????
	count=$(ls -d ${current_date}_* 2>/dev/null | wc -l)
	new_num=$((count + 1))
	new_dir="${current_date}_${new_num}"

# 3. ?????
	mkdir "$new_dir"

# 4. ??????????
	target_dir="./$new_dir"

# 5. ?? Makefile ????
	cp Makefile "$target_dir/"

	echo "mkdir over"
